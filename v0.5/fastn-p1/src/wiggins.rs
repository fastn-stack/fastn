/// ender takes the linear fastn_p1::Section vector as parsed by our first phase, and
/// follows the rule about `-- end: <section-name>` to create a nested structure.
///
/// can we do it in place, instead of returning a new vector?
pub fn ender(_linear: Vec<fastn_p1::Section>) -> Vec<fastn_p1::Section> {
    todo!()
}
