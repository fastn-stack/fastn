-- fbt:
cmd: cd amitu && $FBT_CWD/target/debug/fpm --test sync index.ftd nested/document.ftd && $FBT_CWD/target/debug/fpm --test sync
output: amitu/.history
skip: `fpm sync` is not supporting offline mode

-- stdout:


Repo for amitu is github, directly syncing with .history.
index.ftd
nested/document.ftd
Repo for amitu is github, directly syncing with .history.
FPM.ftd
nested/index.ftd

