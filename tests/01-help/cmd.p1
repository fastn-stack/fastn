-- fbt:
cmd: $FBT_CWD/target/debug/fpm --test --help

Will have to be updated every time version changes.

-- stdout:

FTD Package Manager 0.1.4
Amit Upadhyay <upadhyay@gmail.com>:Shobhit Sharma <shobhit@fifthtry.com>:Arpita Jaiswal <arpita@fifthtry.com>
Description...

USAGE:
    fpm [FLAGS] [SUBCOMMAND]

FLAGS:
    -h, --help       Prints help information
    -V, --version    Prints version information
    -v               Sets the level of verbosity

SUBCOMMANDS:
    build     Builds the current directory
    check     Checks the folder structure of the current .FPM.ftd file
    diff      Show changes in files, comparing it with latest version of that file in `.history` folder
    help      Prints this message or the help of the given subcommand(s)
    mark      Marks the file up to date
    status    Shows how many files have changed, comparing it with latest version of that file in `.history` folder
    sync      `sync` with `fpm-repo` or `.history` folder if not using `fpm-repo`
    tracks    Tracks the document

