-- ft.page-with-toc: Upgrade `fpm`
toc: $ft.toc

-- ft.markdown: 


As of Rust 1.41.0, `cargo install fpm` does an auto upgrade of eg `fpm` if it
is already installed.

If you are using older Rust, then run the following to upgrade.


-- ft.code:
lang: sh

cargo install --force fpm
