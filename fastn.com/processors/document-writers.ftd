-- ds.page: `document-writers` processor

`document-writers` processor is used to find all the writers
[groups](/dev/user-group/) by `document`.


-- ds.code:
lang: ftd

\-- fastn.user-group-compat group:
$processor$: document-writers
document: /

-- ds.markdown:

This will initialize the `group` variable with group who have permission to
write in passed `document`.

Check type [user-group-compat](/theme/fastn/#user-group-compat)

-- end: ds.page