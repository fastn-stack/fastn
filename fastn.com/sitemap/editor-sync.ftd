-- ft.page: `editor-sync` API

`editor-sync` API is called from `fpm editor` to sync the local content with
remote server. It worked same as [`fpm sync`](/sitemap/sync/) worked.

-- ft.h1: URL and Parameters

The URL is /-/editor-sync/` with HTTP GET method.
