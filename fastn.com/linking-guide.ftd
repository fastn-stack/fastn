-- ds.page: `ftd` linking guide

-- ds.markdown:

We put all links without slash in front. And we always put slash in end.

Example:

Instead of using `[fastn build](/build/)` or `[fastn build](/build)`, use like `[fastn build](build/)`

-- end: ds.page