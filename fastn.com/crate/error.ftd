-- ft.page-with-toc: `fpm::Error`
toc: $config.crate-toc
header-left: $config.dev-section
header-right: $config.empty

`fpm::Error` is the main Error enum that is returned by most of our functions.

-- ft.code:
lang: rs

pub enum Error {
    HttpError(Error),
    IoError(Error),
    ZipError(ZipError),
    FTDError(Error),
    PackageError {
        message: String,
    },
    UsageError {
        message: String,
    },
    IgnoreError(Error),
    FromPathBufError(FromPathBufError),
}

