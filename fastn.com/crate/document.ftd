-- ds.page: `fpm::Document`

`fpm::Document` struct keeps track of a "document". Document is a [`file` in fpm
package](crate/file/), which is either a ftd file, a markdown file, or a code
file.

-- ds.code:
lang: rs

#[derive(Debug, Clone)]
pub struct Document {
    pub package_name: String,
    pub id: String,
    pub content: String,
    pub parent_path: String,
}

-- end: ds.page