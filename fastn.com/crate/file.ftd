-- ds.page: `fastn::File`

`fastn::File` enum represents a file found in fastn package.

-- ds.code:
lang: rs

#[derive(Debug, Clone)]
pub enum fastn::File {
    Ftd(fastn::Document),
    Static(fastn::Static),
    Markdown(fastn::Document),
    Code(fastn::Document),
    Image(fastn::Static),
}

-- ds.markdown:

[`fastn::Document`](crate/document/). [`fastn::StaticFile`](crate/static-file/).

-- end: ds.page