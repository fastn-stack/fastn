-- fpm.sitemap-data sitemap:
$processor$: get-sitemap

-- ft.page-with-toc: `fpm::Result`
toc: $sitemap.toc
sub-sections: $sitemap.subsections
extra: $config.empty

Most `fpm` operations return `fpm::Result<T>`.

-- ft.code:
lang: rs

pub type fpm::Result<T> = std::result::Result<T, fpm::Error>;

-- ft.markdown:

`fpm::Result` is a wrapper over [`fpm::Error`](crate/error/).
