-- ds.page: `fastn::Result`

Most `fastn` operations return `fastn::Result<T>`.

-- ds.code:
lang: rs

pub type fastn::Result<T> = std::result::Result<T, fastn::Error>;

-- ds.markdown:

`fastn::Result` is a wrapper over [`fastn::Error`](crate/error/).

-- end: ds.page