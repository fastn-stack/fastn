-- ds.page: Available `$processor$`s

-- ds.markdown: 

FPM comes with following built in `ftd` `$processor$`s.

- [`$processor$: http`](http/)
- [`$processor$: toc`](toc/)
- [`$processor$: package-query`](package-query/)

FPM also comes with a special document [`fpm`](fpm-vars/) that can be
imported by any document, which contains helpful variables.

-- end: ds.page