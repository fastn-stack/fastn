-- import: fifthtry.github.io/Jupiter-theme as ft
-- import: config



-- ft.doc-page:
toc: $ft.toc


-- ft.h0: `fpm update`

`fpm update` updates all the dependencies of the package.

Note: Currently we simply delete `.packages` folder and re-download everything, so
do not do any modifications in `.packages` or do not run this command.
