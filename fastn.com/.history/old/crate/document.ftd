-- my-ds.page: `fastn::Document`

`fastn::Document` struct keeps track of a "document". Document is a [`file` in fastn
package](crate/file/), which is either a ftd file, a markdown file, or a code
file.

-- ds.code:
lang: rs

#[derive(Debug, Clone)]
pub struct Document {
    pub package_name: String,
    pub id: String,
    pub content: String,
    pub parent_path: String,
}

-- end: my-ds.page
