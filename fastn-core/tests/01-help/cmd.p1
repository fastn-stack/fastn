-- fbt:
cmd: $FBT_CWD/../target/debug/fastn --test --help

Will have to be updated every time version changes.

-- stdout:

Usage: fastn [OPTIONS] [COMMAND]

Commands:
  create-package    Create a new fastn package
  build             Build static site from this fastn package
  test              Run the test files in `_tests` folder
  query             JSON Dump in various stages
  clone             Clone a package into a new directory
  add               Add one or more files in the workspace
  rm                Removes one or more files from the workspace
  update            Reinstall all the dependency packages
  sync              Sync with fastn-repo (or .history folder if not using fastn-repo)
  create-cr         Create a Change Request
  resolve-conflict  Show un-synced changes to files in this fastn package
  serve             Serve package content over HTTP
  publish-static    Publish fastn package statically
  help              Print this message or the help of the given subcommand(s)

Options:
  -c, --check-for-updates  Check for updates
  -v                       Sets the level of verbosity
  -h, --help               Print help
  -V, --version            Print version
