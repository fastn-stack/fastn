-- fbt:
cmd: $FBT_CWD/../target/debug/fastn --test sync-status && $FBT_CWD/../target/debug/fastn --test resolve-conflict --delete-it index.ftd
output: .clone-state
skip: resolve_conflict is not yet implemented

-- stdout:

CloneEditedRemoteDeleted: index.ftd
