-- fbt:
cmd: $FBT_CWD/../target/debug/fastn --test sync-status && $FBT_CWD/../target/debug/fastn --test resolve-conflict --use-ours index.ftd
output: .clone-state
skip: resolve_conflict is not yet implemented

-- stdout:

Conflicted: index.ftd
